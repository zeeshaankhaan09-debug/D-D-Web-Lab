# D&D Web Lab - Robots.txt
# This file governs how search engine bots crawl your site.

# 1. Allow all common web crawlers to access the entire site
User-agent: *
Allow: /

# 2. Add an explicit mention of the sitemap
# This ensures that even if a bot misses the auto-discovery, it finds your map.
Sitemap: https://deenanddestination.site/sitemap.xml

# 3. Optional: Specific instructions for Google (the primary driver for Maps traffic)
User-agent: Googlebot
Allow: /

# 4. Optional: Specific instructions for AI crawlers
# Since you WANT AI bots to read your site, we keep them allowed.
# This prevents them from being confused by your file structure.
User-agent: ChatGPT-User
Allow: /
User-agent: Google-Extended
Allow: /
